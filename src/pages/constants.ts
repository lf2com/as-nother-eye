export enum CatcherMessage {
  catch = 'catch',
  nextCamera = 'next-camera',
  flipX = 'flip-x',
  flipY = 'flip-y',
}

export enum ViewerMessage {
  catchStart = 'catch-start',
  catchEnd = 'catch-end',
  nextCameraStart = 'next-camera-start',
  nextCameraEnd = 'next-camera-end',
}
